var has_announced = false;

function init() {
    log("Starter datapack loaded");
}

function main() {
    if (!has_announced) {
        tellraw("@a", text().text("Welcome from Minecraft-Script!").color("gold").bold());
        set has_announced = true;
    }
}

function kill() {
    log("Starter datapack disabled");
}
